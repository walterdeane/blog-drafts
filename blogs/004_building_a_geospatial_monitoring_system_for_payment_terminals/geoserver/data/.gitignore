# Runtime/volatile files generated by GeoServer - everything else (workspaces,
# stores, layers, styles, layer groups, security config) is checked in so the
# stack comes up fully configured.
logs/
tmp/
temp/
work/
data/tiles/
gwc/
geowebcache/
*.lock
*.pid
tomcat_pass.txt
