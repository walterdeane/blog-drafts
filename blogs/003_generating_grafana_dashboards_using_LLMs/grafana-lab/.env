REG_NAME=k3d-reg.localhost
REG_PORT=12345
CLUSTER_NAME=grafana-lab
# Option 1: Use Docker Hub (recommended - no insecure registry needed)
REG_URL=docker.io/yourusername
# Option 2: Use local K3D registry (requires insecure registry config)
# REG_URL=k3d-reg.localhost:12345
